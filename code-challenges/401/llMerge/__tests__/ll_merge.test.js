'use strict'

const LinkedList = require('../ll_merge.js/');

describe('test my mergedList', () => {
    test('testing that my mergeList method zips two lists together', () => {
        let list1 = new LinkedList(1, 2, 3);
        let list2 = new LinkedList(a, b, c);
    })
})